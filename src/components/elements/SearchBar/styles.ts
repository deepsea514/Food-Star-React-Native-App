import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  searchContainer: {
    padding: 10,
    paddingTop: 8,
    paddingBottom: 8,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 1,
    },
    shadowOpacity: 0.02,
    shadowRadius: 1.0,
    minHeight: 60,
    elevation: 1,
  },
});
